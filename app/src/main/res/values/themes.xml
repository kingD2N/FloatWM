<resources>
    <style name="Theme.FloatWM" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!-- Used only by MainActivity, which never shows real content; this is
         what makes "no full-screen UI" literally true rather than just a
         zero-content screen. -->
    <style name="Theme.FloatWM.Transparent" parent="Theme.FloatWM">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
