// Top-level build file. Android Studio will offer to bump these to whatever
// it currently bundles the first time you open the project -- accept that,
// these are just a known-good recent baseline.
plugins {
    id("com.android.application") version "8.7.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
